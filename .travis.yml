
language: clojure
jdk:
  - oraclejdk8
script:
  - lein clean
  - lein compile
  - lein check
  - lein cljsbuild once
  - lein eastwood
#TODO  - lein test
notifications:
  email:
    on_success: change # default: change
    on_failure: always # default: always
